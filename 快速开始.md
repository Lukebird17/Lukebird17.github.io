# 🚀 快速开始指南

## 方法一：使用自动化脚本（推荐）

直接运行部署脚本：

```bash
./deploy.sh
```

脚本会自动：
- ✅ 初始化 Git 仓库
- ✅ 更新配置文件中的用户名
- ✅ 添加远程仓库
- ✅ 提交并推送代码

## 方法二：手动部署

### 第一步：修改配置文件

编辑 `hugo.toml`，修改以下三处：

1. **第 3 行**：将 `yourusername` 改为你的 GitHub 用户名
```toml
baseURL = "https://yourusername.github.io/"
```

2. **第 2 行**：修改作者名字
```toml
author = "Your Name"
```

3. **第 54 行**：修改 GitHub 链接
```toml
github = "https://github.com/yourusername"
```

### 第二步：创建 GitHub 仓库

1. 登录 GitHub
2. 点击右上角 "+" → "New repository"
3. **仓库名必须是**：`yourusername.github.io`（替换为你的用户名）
4. 选择 Public（公开）
5. 点击 "Create repository"

### 第三步：推送代码

```bash
# 初始化 Git（如果还没有）
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit"

# 添加远程仓库（替换 yourusername）
git remote add origin https://github.com/yourusername/yourusername.github.io.git

# 推送
git branch -M main
git push -u origin main
```

### 第四步：配置 GitHub Pages

1. 打开你的仓库页面
2. 点击 **Settings**（设置）
3. 左侧菜单找到 **Pages**
4. 在 **Source** 下拉菜单选择 **GitHub Actions**
5. 保存

### 第五步：等待部署

- 在仓库的 **Actions** 标签查看部署进度
- 等待 2-3 分钟
- 访问 `https://yourusername.github.io`

## 本地预览

```bash
# 安装 Hugo（macOS）
brew install hugo

# 启动本地服务器
hugo server -D

# 浏览器访问 http://localhost:1313
```

## 添加新文章

```bash
# 创建新文章
hugo new post/my-article.md

# 编辑 content/post/my-article.md

# 提交更新
git add .
git commit -m "Add new post"
git push
```

每次推送后，GitHub Actions 会自动重新部署网站。

## 📚 更多信息

详细文档请查看：`README_DEPLOYMENT.md`

## ❓ 常见问题

**Q: 部署失败怎么办？**
A: 检查仓库的 Actions 标签页，查看错误日志。

**Q: 网站显示 404？**
A: 确保仓库名格式正确，并在 Pages 设置中选择了 GitHub Actions。

**Q: 修改后网站没更新？**
A: 等待几分钟让 GitHub Actions 完成部署，可在 Actions 标签查看进度。

**Q: 如何自定义主题？**
A: 编辑 `hugo.toml` 文件中的参数，或查看主题文档。

---

🎉 祝你使用愉快！如有问题，欢迎查看 [Hugo 文档](https://gohugo.io/) 或 [主题文档](https://github.com/heyeshuang/hugo-theme-tokiwa)。
